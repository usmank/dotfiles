#===============================================================================
# General
#===============================================================================
# Update terminal emulator title
set -g set-titles on

# Enable vi mode
setw -g mode-keys vi

set -g history-limit 10000

set -g default-terminal "xterm-256color"
#set -g default-terminal "${TERM}"
set -ga terminal-overrides ",xterm-256color:Tc"
set -sa terminal-overrides ",alacritty:RGB"
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.c
# Number windows and panes from 1
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

set -g focus-events on
set -g monitor-bell off

#===============================================================================
# Mouse Support
#===============================================================================
set -g mouse on

# -----------------------------------------------------------------------------
# Color Scheme
# -----------------------------------------------------------------------------
# Catppuccin Latte
base=#eff1f5
blue=#1e66f5
crust=#dce0e8
flamingo=#dd7878
green=#40a02b
lavender=#7287fd
mantle=#e6e9ef
maroon=#e64553
mauve=#8839ef
overlay0=#9ca0b0
overlay1=#8c8fa1
overlay2=#7c7f93
peach=#fe640b
pink=#ea76cb
red=#d20f39
rosewater=#dc8a78
sapphire=#209fb5
sky=#04a5e5
subtext0=#6c6f85
subtext1=#5c5f77
surface0=#ccd0da
surface1=#bcc0cc
surface2=#acb0be
teal=#179299
text=#4c4f69
yellow=#df8e1d

# Catppuccin Mocha
# base=#1e1e2e
# blue=#89b4fa
# crust=#11111b
# flamingo=#f2cdcd
# green=#a6e3a1
# lavender=#b4befe
# mantle=#181825
# maroon=#eba0ac
# mauve=#cba6f7
# overlay0=#6c7086
# overlay1=#7f849c
# overlay2=#9399b2
# peach=#fab387
# pink=#f5c2e7
# red=#f38ba8
# rosewater=#f5e0dc
# sapphire=#74c7ec
# sky=#89dceb
# subtext0=#a6adc8
# subtext1=#bac2de
# surface0=#313244
# surface1=#45475a
# surface2=#585b70
# teal=#94e2d5
# text=#cdd6f4
# yellow=#f9e2af

# Gruvbox dark
# base=#282828
# blue=#458588
# crust=#1d2021
# flamingo=#fb4934
# green=#98971a
# lavender=#83a598
# mantle=#3c3836
# maroon=#cc241d
# mauve=#b16286
# overlay0=#665c54
# overlay1=#7c6f64
# overlay2=#928374
# peach=#d65d0e
# pink=#d3869b
# red=#fb4934
# rosewater=#fbf1c7
# sapphire=#689d6a
# sky=#83a598
# subtext0=#bdae93
# subtext1=#d5c4a1
# surface0=#32302f
# surface1=#3c3836
# surface2=#504945
# teal=#8ec07c
# text=#ebdbb2
# yellow=#fabd2f

# Solarized light
# base=#fdf6e3
# blue=#268bd2
# crust=#eee8d5
# flamingo=#dc322f
# green=#859900
# lavender=#6c71c4
# mantle=#eee8d5
# maroon=#cb4b16
# mauve=#6c71c4
# overlay0=#93a1a1
# overlay1=#839496
# overlay2=#657b83
# peach=#cb4b16
# pink=#d33682
# red=#dc322f
# rosewater=#eee8d5
# sapphire=#268bd2
# sky=#2aa198
# subtext0=#586e75
# subtext1=#657b83
# surface0=#eee8d5
# surface1=#e0dac3
# surface2=#d6cfb9
# teal=#2aa198
# text=#586e75
# yellow=#b58900

# -----------------------------------------------------------------------------
# Status bar
# -----------------------------------------------------------------------------
# Message command and mode style
set -g message-style bg=default,fg=default
set -g message-command-style bg=default,fg=default,reverse
set -g mode-style bg=default,fg=default,reverse
set -g copy-mode-match-style bg=colour12
set -g copy-mode-current-match-style reverse

# Status bar style
set -g status-justify left
set -g status-style bg=default,fg=default,none
set -g status-interval 1

# Windows list formatting
set -g status-style bg=default
set -g window-status-style 'bg=default,fg=default,list=focus,none'
set -g window-status-format ' #[bg=#{mantle}] #(~/code/info-scripts/map-numbers.sh #I #F)#W #[bg=default]'
set -g window-status-current-style 'bg=default,fg=default,list=focus'
set -g window-status-current-format ' #[bg=#{mantle},fg=#{blue},reverse,bold] #(~/code/info-scripts/map-numbers.sh #I #F)#W #[bg=default]'
set -g window-status-activity-style bg=default,fg=colour6,blink
set -g window-status-bell-style bg=default,fg=colour6,blink
set -g window-status-separator ''

# Left status formatting
#set -g status-left-style bg=colour250,fg=default,bold
#set -g status-left ' #{client_session} #[bg=colour254,fg=default,none] #(~/code/info-scripts/ssh-info.sh #{pane_tty}) '
set -g status-left ''
set -g status-left-length 0

# Right status formatting
set -g status-right-style 'bg=default,fg=default,none'
set -g status-right '\
#[bg=#{mantle},fg=default] 󰟀#(bash ~/code/info-scripts/ssh-info.sh #{pane_tty}) #[bg=default] \
#[bg=#{mantle},fg=default] 󰯌#{session_name} #[bg=default] \
#[bg=#{mantle},fg=default] 󱐋#(bash ~/code/info-scripts/power-info.sh #{pane_tty}) #[bg=default] \
#[bg=#{mantle},fg=default] 󰃰%a %b %d %H:%M '
set -g status-right-length 80

# Pane border status
set -g pane-border-style fg=${mantle}
set -g pane-active-border-style fg=${overlay0}
set -g pane-border-status off
set -g pane-border-format ' #P '

# Pane number display
set -g display-panes-colour colour7
set -g display-panes-active-colour colour4

# Clock colour
set -g clock-mode-colour colour0

#===============================================================================
# Keybinds
#===============================================================================
# Change prefix to Ctrl-a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Reload config file
unbind r
bind r source-file ~/.tmux.conf

# Display pane numbers on Ctrl-a + Ctrl-a
bind C-a display-panes

# Reduce delay after pressing escape
set -sg escape-time 0

# Switch to next session
bind k switch-client -n

# Move windows left/right
bind -n C-S-Left swap-window -t -1\; select-window -t -1
bind -n C-S-Right swap-window -t +1\; select-window -t +1

# Split panes switch to current directory
bind '"' split-window -v -c '#{pane_current_path}'
bind % split-window -h -c '#{pane_current_path}'

# Swtich to next session
bind k switch-client -n

#===============================================================================
# Smart pane switching with awareness of vim splits
#===============================================================================
# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l

